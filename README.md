# SQL

SQL (Structured Query Language) is a standard programming language used to store, retrieve, manage, and manipulate data in a relational database.
It is used to:
1.Create databases and tables
2.Insert, update, delete data
3.Fetch data using queries
4.Apply conditions, sorting, grouping, and filtering
5.Control access and maintain data security
